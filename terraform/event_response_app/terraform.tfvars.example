ecs_cluster_arn   = "arn:aws:ecs:REGION:ACCOUNT:cluster/YOUR_CLUSTER"
vpc_id            = "vpc-xxxxx"
private_subnet_ids = ["subnet-xxx", "subnet-yyy", "subnet-zzz"]
public_subnet_ids  = ["subnet-aaa", "subnet-bbb"]
environment       = "dev"
desired_count     = 1

# Image built from app/ (e.g. docker build -t event-response-app:latest app/ then push to ECR)
web_image         = "ACCOUNT.dkr.ecr.REGION.amazonaws.com/event-response-app:latest"
web_port          = 8080

# Optional: FastSchema secrets (same as fastschema stack)
# fastschema_app_key        = "..."
# fastschema_admin_username = "admin"
# fastschema_admin_password = "..."
